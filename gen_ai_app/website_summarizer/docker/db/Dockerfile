# Dockerfile for db service
FROM postgres:15

# Environment variables for PostgreSQL
ENV POSTGRES_USER=ai_chat_user
ENV POSTGRES_PASSWORD=securepassword
ENV POSTGRES_DB=ai_chat_db

# Expose the PostgreSQL port
EXPOSE 5432
